# SensorProof

Deterministic, fault-aware sensor fusion for robotics with independently replayable decision certificates.

This repository preserves and repurposes the previously empty `codex-test` repository. The first portfolio-grade release is being developed through a reviewed feature branch.
